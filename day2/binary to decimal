#include <stdio.h>
#include<math.h>
int main() {
    int binary,decimal=0,i,n,rem=0;
    scanf("%d",&binary);
    for(i=0;binary!=0;i++)
    {
        rem=binary%10;
        binary= binary/10;
        decimal = decimal +( rem * pow(2,i));
    }
    printf("%d",decimal);

    return 0;
}
