#include <stdio.h>

int main()
{
 int n,i,sum;
 scanf("%d",&n);
 if(i<=20)
 {
     while(i<=n)
     {
         sum = sum+(i*i*i);
         i =i+2;
     }
     printf(" %d %d",n,sum);
 
}
 

    return 0;
}
